all:
    hosts:
        remoment_server:
            ansible_host: 192.168.1.100      # Change to your server's IP or hostname
            ansible_user: ubuntu              # Change to your SSH user
            # ansible_ssh_private_key_file: ~/.ssh/id_rsa
            # ansible_become_password: "secret"  # or use ansible-vault
