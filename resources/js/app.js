import './bootstrap';

// Alpine is started by Livewire — do not call Alpine.start() here.
// Import and expose Alpine for use in non-Livewire contexts if needed.
import Alpine from 'alpinejs';
window.Alpine = Alpine;
